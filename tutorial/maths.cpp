#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int x = 5, y = 10.5;
    auto max_value = max(x, y);
    cout << max_value << endl;
    cout << acos(0.65) << endl;
    cout << pow(y, x);

    return 0;
}